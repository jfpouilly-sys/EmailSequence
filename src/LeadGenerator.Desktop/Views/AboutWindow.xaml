<Window x:Class="LeadGenerator.Desktop.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Lead Generator"
        Height="500"
        Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#888888" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Product Logo and Name -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <!-- Simple email icon using TextBlock with Unicode character -->
            <TextBlock Text="✉"
                      FontSize="64"
                      Foreground="#2196F3"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,10"/>
            <TextBlock Text="{Binding ProductName}"
                      FontSize="28"
                      FontWeight="Bold"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,5"/>
            <TextBlock Text="{Binding Description}"
                      FontSize="14"
                      Foreground="#666666"
                      HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Version Information -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Version Information"
                          FontSize="16"
                          FontWeight="SemiBold"
                          Margin="0,0,0,10"
                          Foreground="#333333"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Version:" FontWeight="SemiBold" Margin="0,5" Foreground="#555555"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Version}" Margin="10,5" Foreground="#333333"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Build Date:" FontWeight="SemiBold" Margin="0,5" Foreground="#555555"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding BuildDate}" Margin="10,5" Foreground="#333333"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Company:" FontWeight="SemiBold" Margin="0,5" Foreground="#555555"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Company}" Margin="10,5" Foreground="#333333"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text=".NET Version:" FontWeight="SemiBold" Margin="0,5" Foreground="#555555"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding DotNetVersion}" Margin="10,5" Foreground="#333333"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- System Information -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="System Information"
                          FontSize="16"
                          FontWeight="SemiBold"
                          Margin="0,0,0,10"
                          Foreground="#333333"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Machine:" FontWeight="SemiBold" Margin="0,5" Foreground="#555555"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding MachineName}" Margin="10,5" Foreground="#333333"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="OS:" FontWeight="SemiBold" Margin="0,5" Foreground="#555555"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding OperatingSystem}" Margin="10,5" Foreground="#333333"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="User:" FontWeight="SemiBold" Margin="0,5" Foreground="#555555"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding UserName}" Margin="10,5" Foreground="#333333"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Copyright and Close Button -->
        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <TextBlock Text="{Binding Copyright}"
                      FontSize="11"
                      Foreground="#666666"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,15"/>

            <Button Content="Close"
                   Width="100"
                   Height="32"
                   Background="#2196F3"
                   Foreground="White"
                   BorderThickness="0"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Click="CloseButton_Click"
                   HorizontalAlignment="Center"
                   Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#2196F3"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                           CornerRadius="4"
                                           Padding="16,6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
