<Window x:Class="LeadGenerator.Desktop.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="About Lead Generator"
        Height="450"
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Product Logo and Name -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <materialDesign:PackIcon Kind="EmailMultiple"
                                   Width="80"
                                   Height="80"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                                   HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding ProductName}"
                      FontSize="28"
                      FontWeight="Bold"
                      HorizontalAlignment="Center"
                      Margin="0,10,0,5"/>
            <TextBlock Text="{Binding Description}"
                      FontSize="14"
                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                      HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Version Information -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Version Information"
                          FontSize="16"
                          FontWeight="SemiBold"
                          Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Version:" FontWeight="SemiBold" Margin="0,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Version}" Margin="10,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Build Date:" FontWeight="SemiBold" Margin="0,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding BuildDate}" Margin="10,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Company:" FontWeight="SemiBold" Margin="0,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Company}" Margin="10,5"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text=".NET Version:" FontWeight="SemiBold" Margin="0,5"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding DotNetVersion}" Margin="10,5"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!-- System Information -->
        <materialDesign:Card Grid.Row="2" Padding="16">
            <StackPanel>
                <TextBlock Text="System Information"
                          FontSize="16"
                          FontWeight="SemiBold"
                          Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Machine:" FontWeight="SemiBold" Margin="0,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding MachineName}" Margin="10,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="OS:" FontWeight="SemiBold" Margin="0,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding OperatingSystem}" Margin="10,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="User:" FontWeight="SemiBold" Margin="0,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding UserName}" Margin="10,5"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!-- Copyright and Close Button -->
        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <TextBlock Text="{Binding Copyright}"
                      FontSize="11"
                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,15"/>

            <Button Content="Close"
                   Style="{StaticResource MaterialDesignRaisedButton}"
                   Width="100"
                   Click="CloseButton_Click"
                   HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
